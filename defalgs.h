#include <vector>

std::vector<std::vector<int>> genU(int x, int y, int z);
std::vector<std::vector<int>> genL(int x, int y, int z);
std::vector<std::vector<int>> genF(int x, int y, int z);
std::vector<std::vector<int>> genR(int x, int y, int z);
std::vector<std::vector<int>> genB(int x, int y, int z);
std::vector<std::vector<int>> genD(int x, int y, int z);
